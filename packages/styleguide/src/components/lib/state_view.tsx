import {h} from 'preact'

interface ComponentDemoProps {
    children: any
    component: string
    title: string
}

export const StateView = ({state}: ComponentDemoProps) => (
    <div class="c-state-view">


    </div>
)