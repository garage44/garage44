---
description: "Linting workflow - run lint checks before committing"
globs:
  - "**/*.{ts,tsx,js,jsx,css}"
alwaysApply: false
priority: 2
---

# Linting Workflow

Run lint checks before committing code:

```bash
bun run lint:ts
bun run lint:css  # if CSS was modified
```

The git hook will catch linting errors, but fix them proactively. See `eslint.config.js` for rule details.
