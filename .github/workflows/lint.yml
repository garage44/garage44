name: Lint

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    name: Lint Code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Lint TypeScript syntax (oxlint)
        run: bun run lint:ts-syntax

      - name: Lint TypeScript style (eslint)
        run: bun run lint:ts-style

      - name: Lint TypeScript types (tsgo)
        run: bun run lint:ts-types

      - name: Lint CSS
        run: bun run lint:css